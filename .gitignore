# Local secrets — copy src/config/config.example.php to config.php and fill in your own
src/config/config.php

# Photoshop working files — large source files, not needed to run/deploy the site
*.psd

# OS junk
Thumbs.db
.DS_Store

# Editor
.vscode/
.idea/
